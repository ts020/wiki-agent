---
name: commit
description: 変更を git にコミットする。コミット、commit、変更を保存、などの指示で呼び出す。
---

Read `.agents/core.md` from the repository root, then create a git commit following the project commit rule:

- Write the commit message as the reason or intent behind the change.
- Do not merely list changed files or implementation details.
- Do not include AI signatures such as `Co-Authored-By`.
