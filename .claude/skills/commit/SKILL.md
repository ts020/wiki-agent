---
description: 変更をgitにコミットする。コミット、commit、変更を保存、などの指示で呼び出す。
---

以下のルールに従って git commit を作成してください。

## コミットルール

- コミットメッセージは「何を変えたか」ではなく「なぜ変えたか（変更の意図）」を記述すること
- 変更内容の列挙ではなく、その変更が必要になった背景・目的を簡潔に伝える
- コミットに AI 署名（Co-Authored-By 等）を含めない
