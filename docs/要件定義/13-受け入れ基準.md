# 13. 受け入れ基準

- index.md が生成される
- 複数ノードが生成される
- ノード構造が存在する
- 除外対象が無視される
- 任意ノード単体で理解可能
- コードベースが変更されない
- 手書き md を追加して再実行すると、index・タグ索引・バックリンクに反映される
- `[[NoteName]]` リンクが相対パスに解決される、未解決時は `_unresolved.md` に列挙される
- シンボル 100 件超のディレクトリで `_symbols.md` が生成される
- 外部ネットワーク接続なしで完走する
