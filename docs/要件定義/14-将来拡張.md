# 14. 将来拡張

v1 では実装しない。必要になった時点で再検討する。

## 索引の深化
- 見出し索引の h3 以上への対応
- 本文内 `#tag` 記法のサポート（コードスニペット等の誤検出対策を含む）
- タグの正規化・別名（同義語タグの統合）
- 自動キーワードリンク（他ノートのタイトルを本文中から検出してリンク化）

## リンク機能
- `![[NoteName]]` による埋め込み展開
- 双方向リンクの可視化強化（グラフ図）
- 被参照数・参照数によるスコアリングと Related の再ランキング

## 運用
- watch モード（ファイル監視による自動再実行）
- `.gitignore` / `.ignore` 準拠の追加除外
- 生成先の clean オプション
- 出力形式の追加（JSON インデックス、静的 HTML）

## 入力
- `.markdown` 等の別拡張子サポート
- 複数入力パスの同時指定
- フロントマターの他フォーマット対応（TOML, JSON）

## 検索・発見性
- 全文検索インデックス生成
- ローカル埋め込みによる類似ノート推薦
- 日付ベースのタイムライン索引

## AI 連携
- オプションの AI 強化モード（自然文要約）
- LLM 向けの最適化ビュー（コンテキスト圧縮版索引）

## その他
- UI / ビューア同梱
- 共同編集対応
