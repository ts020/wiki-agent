# 6. 前提

- CLI アプリケーションとして動作（対話 UI を持たない）
- 入力はローカルの `.md` ファイルまたはディレクトリ
- 出力は Markdown のみ（HTML 等は生成しない）
- 入力は UTF-8
- 既存の Markdown は CommonMark に準拠しているものとする（Obsidian 方言のうち `[[wikilink]]` のみ特別に扱う）
- 非破壊操作: 入力側のファイルを変更しない
- 外部 API / ネットワーク接続に依存しない（完全オフライン）
- 単一バイナリとして配布可能（Rust で実装）
